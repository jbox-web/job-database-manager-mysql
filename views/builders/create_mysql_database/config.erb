<%
  f = taglib("/lib/form")

  desc = 'Environment variables available in shell build steps: $MYSQL_DATABASE, $MYSQL_USER, $MYSQL_PASSWORD, $MYSQL_HOST, $MYSQL_PORT.'

  f.entry :title => 'Database name', :field => 'job_db_name', :description => desc do
    f.textbox
  end

  f.advanced do
    f.entry :title => 'Database user name', :field => 'job_db_user' do
      f.textbox
    end

    f.entry :title => 'Database user password', :field => 'job_db_pass' do
      f.textbox
    end
  end
%>
